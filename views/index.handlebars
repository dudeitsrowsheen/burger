<div class="main-container">

<div class="jumbotron">
    <h1>Eat Da Burger</h1>
</div>
    {{!-- <div class="row">
        <div class="col-md-12">
            <div>

            </div> --}}
            {{#each items}}
           <p>{{burger_name}}</p>
            {{/each}}